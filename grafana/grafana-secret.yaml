apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: veronica
type: Opaque
data:
  # Base64 encoded admin user and password. Change to your desired credentials
  # To generate: echo -n 'admin' | base64
  admin-user: YWRtaW4=  # 'admin'
  admin-password: a29mb2xsYQ==  # 'kofolla'