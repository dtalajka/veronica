apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: veronica
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
  type: LoadBalancer  # Use LoadBalancer or NodePort if external access is needed