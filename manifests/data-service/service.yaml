apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: veronica
spec:
  selector:
    app: data-service
  ports:
  - port: 8000
    targetPort: 8000
  type: LoadBalancer  # Use LoadBalancer or NodePort if external access is needed