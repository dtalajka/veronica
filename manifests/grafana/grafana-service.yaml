apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: veronica
spec:
  selector:
    app: grafana
  ports:
  - port: 3000
    targetPort: 3000
  type: LoadBalancer  # Use LoadBalancer or NodePort if external access is needed